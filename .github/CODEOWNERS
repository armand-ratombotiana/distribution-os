# CODEOWNERS for Distribution OS.
#
# Syntax: <path-pattern> @owner1 @owner2 [@team]
# GitHub requests review from the listed owners whenever their owned
# paths change in a pull request. The last matching pattern wins.
#
# Repository-wide ownership. @armand-ratombotiana is the sole code
# owner and the required reviewer for every pull request. This single
# catch-all rule keeps the review queue simple while the team is small;
# split it into per-area rules (api, db, lib, ui, docs, ci) once more
# than one maintainer is onboarded.

*    @armand-ratombotiana
